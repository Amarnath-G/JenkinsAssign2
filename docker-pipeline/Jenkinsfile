pipeline{
    agent any
    stages{
        stage('Buid'){
            withCredentials([file(credentialsId: 'privatekey1', variable: 'FILE_PATH')]) {
                sh '''ssh -o StrictHostKeyChecking=no -i $FILE_PATH ec2-user@13.61.100.59 docker ps
                '''
            }
        }
    }
}